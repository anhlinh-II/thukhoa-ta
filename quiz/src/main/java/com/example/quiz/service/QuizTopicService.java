package com.example.quiz.service;

// Temporarily commented out to fix compilation issues
// TODO: Implement properly later

/*
import com.example.quiz.base.baseInterface.BaseService;
import com.example.quiz.model.dto.request.QuizTopicRequestDto;
import com.example.quiz.model.dto.response.QuizTopicResponseDto;
import com.example.quiz.model.entity.QuizTopic;
import com.example.quiz.model.view.QuizTopicView;

import java.util.List;

public interface QuizTopicService extends BaseService<QuizTopic, Long, QuizTopicRequestDto, QuizTopicResponseDto, QuizTopicView> {

    List<QuizTopicResponseDto> findByQuizGroupId(Long quizGroupId);

    List<QuizTopicResponseDto> findActiveByQuizGroupId(Long quizGroupId);

    QuizTopicResponseDto getQuizTopicById(Long id);

    QuizTopicResponseDto findBySlug(String slug);

    QuizTopicResponseDto create(QuizTopicRequestDto requestDto);

    QuizTopicResponseDto update(Long id, QuizTopicRequestDto requestDto);

    void deleteById(Long id);

    void softDeleteById(Long id);

    List<QuizTopicResponseDto> findByPrerequisiteTopicId(String topicId);
}
*/
